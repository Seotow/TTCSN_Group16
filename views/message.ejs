<script>
    <% if (session.message) { %>
        <% if (session.message.type == "success") {%>
            showSuccessMessage('<%= session.message.text %>');
        <% } else {%>
            showErrorMessage('<%= session.message.text %>');
        <% } %>
        <% delete session.message; %>
    <% } %>
</script>